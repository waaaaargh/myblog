<a href="/posts/post_{{post.id}}"><b>{{post.title}}</b><a>:
{% if post.excerpt != None %}
    {{post.excerpt}}
{% else %}
    {{post.content}}
{% endif %} ({{post.date.strftime("%c").decode('utf-8')}})
